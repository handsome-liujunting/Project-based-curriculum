# 个人主页 V3.5 制作流程与记录

> 版本：V3.5（在 V3 基础上新增「访客反馈入口 + 后台收集」）
> 记录日期：2026-09-20
> 源码：`outputs/personal-homepage-v35/`　单文件版：`outputs/personal-homepage-v35-standalone/index.html`
> 上一步基线：`outputs/personal-homepage-v3/`（V3，全程未改动，用作对照与回滚）

---

## 一、本轮任务来源

依据课件 **《个人主页V3-发布反馈与持续改进.pdf》**（44 页，已 OCR 逐页提取要点）：

| 课件要求 | V3.5 的落实 |
|---|---|
| 页面要能收到访客反馈，且**不跳转页面** | 右下角常驻「提个意见」按钮 → 就地弹出模态表单 |
| 反馈入口要让访客**放心写** | 表单内明示「反馈不会公开，只有我能看到。」 |
| 字段：昵称（可选）、关系、设备、内容（必填）、时间与版本号（自动） | 6 个字段按此实现；时间由数据库 `now()` 生成、版本号由 `siteVersion` 自动附带 |
| 后台要有地方存、能查看 | Supabase `feedback` 表 + RLS 只允许匿名插入（作者可在控制台查看） |
| 只能把可公开的密钥放前端 | 仅用 publishable key；secret key / 数据库密码绝不进前端（含自动扫描） |
| 持续改进：知道问题出在哪一版 | 每条反馈自动带 `version` 字段 |

---

## 二、关键决策与理由

| 决策 | 理由 |
|---|---|
| **反馈样式单独成文件** `css/feedback.css`，不改 `css/style.css` | 一眼能看出"这一版改了什么"；也避免动到已经稳定的 V3 样式（`style.css` 26149 B 全程未改） |
| **不引入 supabase-js，用原生 `fetch` 打 PostgREST** | 只需要"插入一行"这一个动作；不引第三方库，页面运行时依然零外部依赖，符合本站一贯的"零外链"要求 |
| **未配置后台时明确说"通道没接好"，不假装成功** | 课件强调不要让访客以为反馈发出去了；宁可明说，也不给假成功提示 |
| **今天只做前端，Supabase 明天接入** | 你的决定；配置项留空（`url`/`anonKey`），页面照常可用，表单会提示"通道待接入" |
| **站点源码放仓库根目录，历史版本留 `outputs/`** | GitHub Pages 只认根目录或 `docs/`；放根目录后访问仓库地址即主页，历史版本仍作为过程证据保留 |
| **文案逐字照用户原文写入，末尾不加句号** | 你的明确要求；如需统一标点可后续批量处理 |
| **新增汉字后必须重建字体子集** | 子集是静态的，否则新字回退系统字体会"鹤立鸡群" |

---

## 三、文件清单与规模

```
outputs/personal-homepage-v35/
├── index.html                        20 590 B   ← 含 L7 内联脚本（淡入兜底）、L377-379 三个脚本
├── css/style.css                     26 149 B   ← 沿用 V3，未改动
├── css/feedback.css                   6 729 B   ← V3.5 新增
├── js/main.js                         5 044 B   ← 沿用 V3 + 本轮新增淡入兜底
├── js/feedback.js                     5 517 B   ← V3.5 新增
├── js/feedback-config.js              1 472 B   ← V3.5 新增（唯一需要手动填的文件）
└── assets/fonts/                                ← 两个 woff2 + 两份 OFL 授权全文 + README
    ├── bangers-latin.woff2            8 648 B
    ├── zcool-kuaile-subset.woff2     30 452 B
    ├── OFL-Bangers.txt                4 479 B
    ├── OFL-ZCOOL-KuaiLe.txt           4 398 B
    └── README.md                      3 202 B

outputs/personal-homepage-v35-standalone/
├── index.html                       126 313 B   ← 单文件交付版（CSS/JS/字体全内联，base64 字体）
└── index-nojs.html                  126 585 B   ← 白屏排查用的诊断快照（非正式交付物）
```

**全站二进制素材共 2 个（都是字体）**，无任何图片、音视频。详见 `docs/V3.5-素材许可台账.md`。

---

## 四、实施记录（提交链）

| 提交 | 内容 |
|---|---|
| `7dc0878` | **feat: 个人主页 V3.5**（反馈入口前端 + Supabase 配置占位 + 字体子集重生成）—— 11 files / 2093 insertions |
| `89db405` | content: 第二格改为"打球、听歌、观看纪录片"，并重生成字体子集 |
| `d21c4b1` | content: 我的台词新增 3 条（vibe coding / 书籍 / 音乐），并重生成字体子集 |
| `880c0af` | content: 台词只保留后半三条，并重排左右交替，重生成字体子集 |
| `7e75514` | fix: 淡入脚本加兜底防白屏；新增单文件导出脚本与 V3.5 单文件产物 |
| `2cf3b79` | docs: V3.5 建表 SQL、后台接入说明、发布检查清单、素材台账、制作记录；校验与子集脚本入库 `tools/` |
| `c5b060e` | chore: 站点源码同步到仓库根目录，成为可发布结构 —— 13 files / 2286 insertions / 5 deletions |
| `82d2ba7` | docs: 补充根目录同步流程（`sync_site -SiteOnly`）与提交链记录 |
| `ecd499e` | content: 删除"我的台词"整块；重生成字体子集（≤274 字形）；重导单文件；同步仓库根目录 |
| `2fdafbc` | feat: 数字分身（预设问答版）第一版：入口在 03 作品卡片里 —— 15 files / 1749 insertions |

### 数字分身：从卡片入口到独立专区（`2fdafbc` 之后的改动）

需求：让别人能跟"数字分身"聊天。用户选了**预设问答版**——不接大模型，因为
GitHub Pages 是纯静态托管，接大模型要么暴露密钥要么额外付费，而预设问答
**零成本、零网络请求、不怕接口挂掉**（对比 `feedback.js` 是全站唯一会联网的文件）。

第一版把入口做成 03 作品区卡片里的按钮；用户随后选定**做成独立板块，占原来"我的台词"的位置**，
并把分身的调子定为**"温和但有距离"**（愿意答，也始终留着一层）。据此调整：

- 03 作品区那张"数字分身"卡片**移除**（功能已独立成区，留着会和"静态原型"的旧描述矛盾）；
  作品区仍是 5 张面板。
- 新增 **04 数字分身专区**（粉色区 `section--pink` + `id="twin"`），沿用原本"我的台词"的配色位，
  于是页面配色节奏保持不变：**蓝（故事）→ 黄（技能）→ 蓝（作品）→ 粉（分身）→ 红（联系）**。
  卡片内容：🌙 头像 + 标签「INFJ · 没谈过恋爱 · 爱读近代史」+ 一句话 + 按钮「找他聊两句」。
- 「联系我们」区块编号顺延为 **05 联系**；顶部导航与页脚导航都加了「分身」。
- 7 条回答全部按"温和但有距离"重写，例如"你平时读什么"→
  「近代史。读得越多越不敢下判断，同一件事总有两三种讲法，都站得住脚。」
- 深链拆成两个：`#twin` 只滚到专区（留给导航），`#twin-chat` 才打开对话框。
- 分身台词写在 `js/digital-twin.js` 顶部的 `TWIN` 对象里，**改台词只改这一个对象**；
  这些文字用的是正文系统字体（像聊天记录），所以**不需要**重建字体子集
  （只有使用展示字体的文本才要，详见 `assets/fonts/README.md`）。

### 顺手修掉的两个真实隐患：脚本"写死文件清单"

`verify_v35.ps1` 的第 2 节和 `export_single_file.ps1` 的内联清单，原来都把
`style.css / feedback.css / main.js / feedback.js / feedback-config.js` **写死在代码里**。
新增 `digital-twin.css / js` 时后果立刻显现：

1. `verify` 的"每个样式/脚本都被引用"检查**根本不看新文件**——将来忘了在 HTML 里
   引用它，脚本也不会报警；
2. `export_single_file.ps1` 不会内联它们，导出被它自己的保险检查拦下
   （`leftover local reference in single file`）——这次正是这个保险救了一次。

两个脚本现已改为**自动发现** `css\`、`js\` 下的全部文件，以后再加文件不用改脚本。

### 同步到仓库根目录（`c5b060e`）

- 新增 `tools/sync_site.ps1`：把 `outputs/personal-homepage-v35/` 的 `index.html`、`css\`、`js\`、`assets\`
  同步到仓库根（Pages 只认根或 `docs/`）。脚本**只覆盖同名文件、从不删除**，并报告 stale 文件供人工判断。
- 同步结果：11 个发布文件与源目录 `Get-FileHash -Algorithm MD5` **全部 SAME**（逐文件比对通过）。
- 根目录 `verify_v35.ps1 -Root "." -SiteOnly` → `FAIL count = 0`（19 ids 唯一、标签配对全对、
  脚本顺序 main → config → feedback、零外链、零 BOM、配置代码里无密钥）。
- 修掉两个脚本缺陷：
  1. `verify_v35.ps1` 新增 `-SiteOnly`：根目录直接扫会把 `uploads/`（含 400 KB 参考课件 HTML）、
     `outputs/` 里的历史版本一起扫描，输出上千行噪音；加上开关后只扫站点文件（21 → 十几行）。
  2. `sync_site.ps1` 复制目录时用 `Copy-Item -LiteralPath <dir> -Destination <dir> -Recurse`，
     目标已存在时会**嵌套复制**成 `css\css\`；改为 `Copy-Item -Path "$s\*"` 只复制内容。
     该缺陷已由 stale 检查捕获并清理（三个嵌套目录，删除前经用户确认）。
- 校验过的第一条纪律：**脚本里的 PowerShell 函数必须先定义再调用**（`Say` 曾因此报错）。

每次改文案后的固定流水线：**改 HTML → 重跑字体子集（`resubset.ps1`）→ `verify_v35.ps1 -Root "outputs\personal-homepage-v35"`（FAIL=0）→ 重导单文件 → `sync_site.ps1` 同步到仓库根 → 再跑一次 `verify_v35.ps1 -Root "." -SiteOnly` → 提交**。

---

## 五、文案修改记录（均按用户原文）

| 轮次 | 位置 | 改动 |
|---|---|---|
| 1 | 第二格自我介绍 | 「课堂之外，我爱唱跳 rap 篮球。」→ **「课堂之外我还喜欢打球，听歌，观看纪录片」** |
| 2 | 我的台词 | **新增** 3 条：🤖 让我上头的 vibe coding / 📖 书籍是人类进步的阶梯 / 🎧 音乐是我重要的放松方式 |
| 3 | 我的台词 | **删除** 原有的前三条（🧑‍💻 写代码 / 📚 读文史哲 / 🎤 舞台和球场），只留新加的三条，并重新排成 **左 → 右 → 左** 交替 |
| 4 | 我的台词 | **整块删除**：该区块（含 `.voices` 区块结构与说明文案）已从页面移除，位置改由 04 数字分身专区顶上 |

> 第 4 轮之后，`.voices` / `.voice` / `.actor--round` 的样式仍留在 `css/style.css` 里（未清理），
> 以便与 V3 逐行 diff；02 故事区仍在用 `.bubble` / `.actor`，所以这些类不能删。

**字体子集随文案变化的对照（可用于判断子集是否真的重建了）**：

| 节点 | 汉字数 | zcool woff2 |
|---|---|---|
| V3.5 初版 | 284 | 29 464 B |
| 第二格改文案后 | 288 | 29 952 B |
| 台词 +3 条后 | 300 | 31 272 B |
| 删除前三条台词后 | 293 | 30 452 B |
| 删除"我的台词"整块后 | 274 | 28 352 B |
| 数字分身入口（卡片按钮）后 | 281 | 29 128 B |
| 数字分身改为独立专区（当前） | 285 | 29 452 B |

> 分身**对话内容**不在此列：它写在 JS 里且用正文系统字体，不占展示字体的字形。

---

## 六、反馈功能的实现要点

**交互链路**：点按钮 → 打开模态（不跳转、不刷新）→ 填写 → 校验 → `fetch` POST → 提示结果。

| 点 | 做法 |
|---|---|
| 打开/关闭 | `hidden` 属性切换；关闭方式三种：右上角 `×`、点遮罩、按 `Esc`；关闭后焦点回到入口按钮 |
| 无障碍 | `role="dialog"` + `aria-modal="true"` + `aria-labelledby/describedby`；打开时焦点给面板本身（键盘可用，又不会在手机上直接弹键盘）；状态区 `role="status" aria-live="polite"` |
| 必填校验 | 前后端各一层：前端提示"内容还是空的/选一下设备"；数据库 `message not null` + 长度约束兜底 |
| 防重复提交 | `busy` 标记 + 提交中禁用按钮 |
| 失败不丢内容 | 失败时**不清空**表单，提示"内容我帮你留着了，可以再点一次" |
| 超时 | `AbortController` + 12 秒超时，避免一直转圈 |
| 深链 | 网址后加 `#feedback` 自动打开表单（方便分享/截图，且不污染浏览器历史） |
| 滚动锁 | 打开时给 `body` 加 `fb-locked`（`overflow:hidden`） |
| 减少动效 | `prefers-reduced-motion: reduce` 时关闭图标闪动与弹出动画 |
| 零依赖 | 不加载 supabase-js，只用原生 `fetch` 打 `POST {url}/rest/v1/feedback`，请求头带 `apikey` / `Authorization: Bearer` / `Prefer: return=minimal` |

**为什么 `Prefer: return=minimal`**：这样插入后不需要 `SELECT` 权限，
正好和"数据库只给匿名访客 INSERT 权限"的策略严丝合缝 —— 少一个权限，就少一个泄露面。

### 数字分身（预设问答）的实现要点

与反馈功能**同一套交互约定**，但**没有任何网络请求**：

| 点 | 做法 |
|---|---|
| 台词在哪 | 全部在 `js/digital-twin.js` 顶部的 `TWIN` 对象：`greeting` / `topics[]`（q 问 a 答）/ `ending`。改台词只改这一处 |
| 对话流程 | 点话题 → 右侧出现访客提问 → 左侧出现"正在想"三点 → 按 `520ms + 回答字数 × 4ms` 延迟后出回答（模拟打字，不是真在算） |
| 问完之后 | 还有话题时按钮是「再问一个」；全问完显示 `ending` + 「从头再问一遍」，可无刷新重来 |
| 无障碍 | 同样 `role="dialog"` + `aria-modal` + `aria-labelledby`；对话区 `role="log" aria-live="polite"`（读屏会念出新回答）；`×` / 遮罩 / `Esc` 三种关闭 + 焦点回退 |
| 深链 | `#twin-chat` 打开对话框；`#twin` 是专区锚点，只滚动不弹窗 |
| 滚动锁 | 打开时给 `body` 加 `dt-locked` |
| 减少动效 | `prefers-reduced-motion: reduce` 时不等待、不显示三点、头像不浮动 |
| 视觉 | 全部 CSS 自绘，**没有位图、没有第三方素材**，🌙 用系统 emoji（不占字体子集） |
| 为什么不用大模型 | 静态托管没法藏密钥，接 API 要么泄露 key 要么持续花钱；预设问答零成本、不会被刷、断网也能演示。想升级成真模型时，只需要把 `reply()` 里取 `TWIN` 的地方换成 `fetch`，界面和交互都不用改 |

---

## 七、白屏问题排查记录（2026-09-20）

**现象**：你在预览时看不到文字、"全是白色的"。

**排查过程与证据**：

| 步骤 | 做法 | 结果 |
|---|---|---|
| 1 | 用 Edge 无头渲染 `file://` 单文件版与 V3 文件夹版，做像素统计 | 均正常：276 KB / 270 KB 彩图，黑描边 13.6%、主蓝 14.9%、浅蓝 9.8% —— **页面代码本身没问题** |
| 2 | 起本地 http 服务并验证 | `GET /index.html → 200`，服务正常 |
| 3 | 打开极简静态诊断页（纯 HTML、零 JS、零外部资源） | ✅ **能正常显示** → `file://`、内联样式、内联 JS 都可用 |
| 4 | 打开单文件版（资源全内联） | ❌ 仍白屏 |
| 5 | 打开「安全模式」版（移除 `给 <html> 加 .js 类` 这一步，完全不依赖 JS 即可见） | ❌ 仍白屏 |

**结论**：本页在 `file://` 与 `http`、分文件与单文件、有 JS 与无 JS **四种组合下均白屏**，
而极简页正常、同一文件用 Edge 渲染正常 →
判定为**内置浏览器渲染本页（大量自定义字体 + 复杂 CSS）的能力限制**，与页面代码无关。

**据此做的代码改动**（对真实浏览器无害、只增不减）：在
`index.html` 的 `<head>` 内联脚本与 `js/main.js` 中各加一道**兜底**：

- `index.html`：若 2 秒内 `main.js` 未就绪，自动移除 `html.js` 类 → 全部内容直接可见；
- `js/main.js`：1.5 秒后，把视口内仍未显示的元素强制标为可见；就绪时置 `window.__revealReady = true`。

这两道兜底解决的是**真实存在的风险**：本站的滚动淡入依赖 `IntersectionObserver` 回调，
若外部脚本被拦截、或页面在后台标签页加载导致回调迟迟不来，内容会一直停留在 `opacity:0`。
加兜底后，最坏情况也只是"没有淡入动画"，而不会白屏。
另新增**单文件版**（资源全内联、不依赖任何子路径），作为最稳的预览与交付形式。

**遗留**：内置浏览器为何渲染不了本页，未再深究（你决定"以后再说"）。
日常预览建议：双击单文件版用系统浏览器打开，或在产物面板里预览该 HTML。

---

## 八、待办

| # | 事项 | 状态 |
|---|---|---|
| 1 | Supabase 建项目 → 跑 `docs/V3.5-建表与RLS.sql` → 填 `feedback-config.js` | ⏸ 需你提供 Project URL + publishable key |
| 2 | 站点源码同步到仓库根目录（`index.html`/`css`/`js`/`assets`） | ✅ 已完成（`c5b060e`，11 文件 MD5 全 SAME、`FAIL=0`） |
| 3 | 推送到 GitHub 并开启 Pages | ⏸ 你说"先不急" |
| 4 | 截图线（桌面 7 张 + 反馈模态 2 张）重新出图 | ⏸ 未收口（分区改动后裁切偏移要重算） |
| 5 | 桌面交付物 `个人主页-V3.5.html` | ⏸ 可用单文件版替代，定型后再导出 |
| 6 | 数字分身台词定稿 | ✅ 已按"温和但有距离"重写 7 条；后续你随时可再改 `TWIN` |
| 7 | 撤掉已暴露的 GitHub PAT（`ghp_2DFN…jUPQ`） | ⏸ 完工必做 |
| 8 | `outputs/personal-homepage-v35-standalone/index-nojs.html`（白屏诊断快照，已过期） | ⏸ 待你决定删除或重新生成 |

---

## 九、如何从零复现这一版

```powershell
# 1) 校验源码结构
powershell -NoProfile -ExecutionPolicy Bypass -File "tools\verify_v35.ps1" -Root "outputs\personal-homepage-v35"

# 2) 重建字体子集（改动过文案才需要；会联网访问 Google Fonts）
powershell -NoProfile -ExecutionPolicy Bypass -File "tools\resubset.ps1" -Root "outputs\personal-homepage-v35" -ExtraFile "tools\extra_display.txt"

# 3) 导出单文件交付版
powershell -NoProfile -ExecutionPolicy Bypass -File "tools\export_single_file.ps1" -Src "outputs\personal-homepage-v35" -OutHtml "outputs\personal-homepage-v35-standalone\index.html"

# 4) 同步到仓库根目录（Pages 发布根）+ 自动校验
powershell -NoProfile -ExecutionPolicy Bypass -File "tools\sync_site.ps1" -Src "outputs\personal-homepage-v35" -Dst "."

# 5) 本地起服务预览（端口 8123）
#    tools\serve_v35.ps1

# 6) 改完文案别忘了根目录再校验一次（避免根目录和源目录脱节）
powershell -NoProfile -ExecutionPolicy Bypass -File "tools\verify_v35.ps1" -Root "." -SiteOnly
```

> 提示：`-SiteOnly` 在根目录是**必须**的 —— 不加会把 `uploads\`（参考课件）、
> `outputs\`（历史版本）一起扫进来，输出上千行噪音。

> `.deepworks/` 已被 `.gitignore` 排除（属于本地工作区）。
> 其中跨版本仍需长期保留的脚本，已单独放进 `tools/`（如 `export_single_file.ps1`）。
